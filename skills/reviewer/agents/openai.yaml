interface:
  display_name: "Buildable Reviewer"
  short_description: "Review prototypes for quality and local-first discipline."
  default_prompt: "Use $buildable-reviewer to review this prototype against its app spec and rubric."
