interface:
  display_name: "Buildable Mobile Builder"
  short_description: "Build local-first Expo prototypes from app specs."
  default_prompt: "Use $buildable-mobile-builder to generate a polished local mobile prototype from this app spec."
