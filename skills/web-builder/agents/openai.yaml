interface:
  display_name: "Buildable Web Builder"
  short_description: "Build local-first web prototypes from app specs."
  default_prompt: "Use $buildable-web-builder to generate a polished local web prototype from this app spec."
