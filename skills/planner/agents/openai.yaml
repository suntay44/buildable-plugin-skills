interface:
  display_name: "Buildable Planner"
  short_description: "Classify prompts and create local-first app specs."
  default_prompt: "Use $buildable-planner to plan a local-first app prototype from my prompt."
