# Notes Screen Graph

```txt
notes workspace
  -> note list (search + tag/collection filter)
  -> note editor (selected note)
  -> create note
  -> delete note
```

## Required States

- populated note list
- selected note in editor
- empty state (no notes yet)
- filtered empty state (search/tag matches nothing)
- unsaved edits reflected live in the list
