# Modals

## Use When

A focused task temporarily interrupts the current screen, such as confirming deletion or editing a compact record.

## Requirements

- close button
- escape key handling where framework support exists
- focus management
- clear primary and secondary actions
- no full workflows hidden inside modals unless the workflow is genuinely short

