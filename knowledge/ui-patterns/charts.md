# Charts

## Use When

The user needs to compare metrics, trends, segments, or status over time.

## Requirements

- Label the metric and time range.
- Show units and deltas where useful.
- Use at most two chart types on one prototype screen.
- Include empty/loading/error states for chart regions.
- Pair charts with a short interpretation or nearby table when the data needs context.

