# Openings — Job Board Starter

A two-pane job board: filterable, searchable job list on the left; job detail with a validated apply form and confirmation on the right. Generated from the Buildable `job-board` golden template.

```bash
npm install
npm run dev
```

Jobs and applications are local state seeded from `lib/sample-jobs.ts`. Applying shows a confirmation — nothing is sent.
