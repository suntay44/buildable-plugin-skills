# Maya Castillo — Portfolio Starter

An editorial portfolio: hero, filterable project grid with case-study previews, about, and contact CTA. Generated from the Buildable `portfolio` golden template.

```bash
npm install
npm run dev
```

All content lives in `lib/sample-projects.ts`. The contact CTA is a `mailto:` link — no form backend.
