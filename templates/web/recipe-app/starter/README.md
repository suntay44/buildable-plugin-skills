# Larder — Recipe App Starter

Browse photo-free recipe cards, search by ingredient, filter by category and diet, open a detail view with numbered steps, and save recipes locally. Generated from the Buildable `recipe-app` golden template.

```bash
npm install
npm run dev
```

Recipes are local state seeded from `lib/sample-recipes.ts`. The save toggle is local — no accounts or sync.
