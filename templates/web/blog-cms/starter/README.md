# Editorial — Blog/CMS Starter

A two-pane content workspace: searchable, filterable post list on the left; full post editor with publish/unpublish on the right. Generated from the Buildable `blog-cms` golden template.

```bash
npm install
npm run dev
```

Posts are local state seeded from `lib/sample-posts.ts`. There is no hosted CMS or publishing integration.
