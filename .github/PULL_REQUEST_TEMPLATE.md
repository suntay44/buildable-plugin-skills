## What this changes

A short description of the change and why.

Closes #

## Type

- [ ] Bug fix
- [ ] New archetype / template
- [ ] Knowledge pack (data model, screen graph, UI pattern, rubric, playbook)
- [ ] CLI / MCP capability
- [ ] Docs / housekeeping

## Checklist

- [ ] `npm test` passes
- [ ] `npm run check` passes
- [ ] `npm run eval` passes
- [ ] `npm run config:check` passes
- [ ] For a new runnable template: it includes `template-spec.json`, `TEMPLATE_PLAN.md`, and a self-contained `starter/` that passes typecheck/build
- [ ] Keeps the core local-first (no hosted platform, billing, accounts, managed databases, telemetry, or deployment requirements)
- [ ] Updated `CHANGELOG.md` under `[Unreleased]` if user-facing
